// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: outbox.sql

package gen

import (
	"context"

	"github.com/jackc/pgx/v5/pgconn"
	"github.com/jackc/pgx/v5/pgtype"
)

const claimPendingOutboxEvents = `-- name: ClaimPendingOutboxEvents :many
WITH candidates AS (
    SELECT id
    FROM outbox_events
    WHERE published_at IS NULL
      AND next_attempt_at <= NOW()
      AND (claimed_until IS NULL OR claimed_until < NOW())
    ORDER BY created_at
    FOR UPDATE SKIP LOCKED
    LIMIT $1
)
UPDATE outbox_events AS events
SET claimed_until = NOW() + ($2 * INTERVAL '1 second')
FROM candidates
WHERE events.id = candidates.id
RETURNING events.id, events.topic, events.payload, events.headers, events.attempts,
          events.next_attempt_at, events.last_error, events.created_at
`

type ClaimPendingOutboxEventsParams struct {
	Limit   int32       `json:"limit"`
	Column2 interface{} `json:"column_2"`
}

type ClaimPendingOutboxEventsRow struct {
	ID            pgtype.UUID        `json:"id"`
	Topic         string             `json:"topic"`
	Payload       []byte             `json:"payload"`
	Headers       []byte             `json:"headers"`
	Attempts      int32              `json:"attempts"`
	NextAttemptAt pgtype.Timestamptz `json:"next_attempt_at"`
	LastError     string             `json:"last_error"`
	CreatedAt     pgtype.Timestamptz `json:"created_at"`
}

func (q *Queries) ClaimPendingOutboxEvents(ctx context.Context, arg ClaimPendingOutboxEventsParams) ([]ClaimPendingOutboxEventsRow, error) {
	rows, err := q.db.Query(ctx, claimPendingOutboxEvents, arg.Limit, arg.Column2)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ClaimPendingOutboxEventsRow{}
	for rows.Next() {
		var i ClaimPendingOutboxEventsRow
		if err := rows.Scan(
			&i.ID,
			&i.Topic,
			&i.Payload,
			&i.Headers,
			&i.Attempts,
			&i.NextAttemptAt,
			&i.LastError,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listPendingOutboxEvents = `-- name: ListPendingOutboxEvents :many
SELECT id, topic, payload, headers, attempts, next_attempt_at, last_error, created_at
FROM outbox_events
WHERE published_at IS NULL
  AND next_attempt_at <= NOW()
  AND (claimed_until IS NULL OR claimed_until < NOW())
ORDER BY created_at
LIMIT $1
`

type ListPendingOutboxEventsRow struct {
	ID            pgtype.UUID        `json:"id"`
	Topic         string             `json:"topic"`
	Payload       []byte             `json:"payload"`
	Headers       []byte             `json:"headers"`
	Attempts      int32              `json:"attempts"`
	NextAttemptAt pgtype.Timestamptz `json:"next_attempt_at"`
	LastError     string             `json:"last_error"`
	CreatedAt     pgtype.Timestamptz `json:"created_at"`
}

func (q *Queries) ListPendingOutboxEvents(ctx context.Context, limit int32) ([]ListPendingOutboxEventsRow, error) {
	rows, err := q.db.Query(ctx, listPendingOutboxEvents, limit)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListPendingOutboxEventsRow{}
	for rows.Next() {
		var i ListPendingOutboxEventsRow
		if err := rows.Scan(
			&i.ID,
			&i.Topic,
			&i.Payload,
			&i.Headers,
			&i.Attempts,
			&i.NextAttemptAt,
			&i.LastError,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const markOutboxEventFailed = `-- name: MarkOutboxEventFailed :execresult
UPDATE outbox_events
SET attempts = attempts + 1, next_attempt_at = $2, last_error = $3, claimed_until = NULL
WHERE id = $1 AND published_at IS NULL
`

type MarkOutboxEventFailedParams struct {
	ID            pgtype.UUID        `json:"id"`
	NextAttemptAt pgtype.Timestamptz `json:"next_attempt_at"`
	LastError     string             `json:"last_error"`
}

func (q *Queries) MarkOutboxEventFailed(ctx context.Context, arg MarkOutboxEventFailedParams) (pgconn.CommandTag, error) {
	return q.db.Exec(ctx, markOutboxEventFailed, arg.ID, arg.NextAttemptAt, arg.LastError)
}

const markOutboxEventPublished = `-- name: MarkOutboxEventPublished :execresult
UPDATE outbox_events
SET published_at = NOW(), last_error = '', claimed_until = NULL
WHERE id = $1 AND published_at IS NULL
`

func (q *Queries) MarkOutboxEventPublished(ctx context.Context, id pgtype.UUID) (pgconn.CommandTag, error) {
	return q.db.Exec(ctx, markOutboxEventPublished, id)
}

const saveOutboxEvent = `-- name: SaveOutboxEvent :exec
INSERT INTO outbox_events (id, topic, payload, headers, attempts, next_attempt_at, last_error, created_at)
VALUES ($1, $2, $3, $4, $5, $6, $7, $8)
`

type SaveOutboxEventParams struct {
	ID            pgtype.UUID        `json:"id"`
	Topic         string             `json:"topic"`
	Payload       []byte             `json:"payload"`
	Headers       []byte             `json:"headers"`
	Attempts      int32              `json:"attempts"`
	NextAttemptAt pgtype.Timestamptz `json:"next_attempt_at"`
	LastError     string             `json:"last_error"`
	CreatedAt     pgtype.Timestamptz `json:"created_at"`
}

func (q *Queries) SaveOutboxEvent(ctx context.Context, arg SaveOutboxEventParams) error {
	_, err := q.db.Exec(ctx, saveOutboxEvent,
		arg.ID,
		arg.Topic,
		arg.Payload,
		arg.Headers,
		arg.Attempts,
		arg.NextAttemptAt,
		arg.LastError,
		arg.CreatedAt,
	)
	return err
}
